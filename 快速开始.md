# StudyX Android APP 快速开始

## 📦 项目结构

```
StudyX-Android/
├── app/src/main/
│   ├── java/com/studyx/app/     # Java 源代码
│   │   ├── MainActivity.java     # 主活动（WebView 容器）
│   │   ├── UpdateManager.java    # 热更新管理器
│   │   └── WebAppInterface.java  # JS 与原生交互接口
│   ├── res/                      # 资源文件（图标、布局等）
│   └── assets/www/               # Web 应用文件（已复制）
├── build_apk.bat                 # 构建脚本（Windows）
├── build_signed.bat              # 构建签名 APK
├── make_update.bat               # 制作热更新包
├── updates/                      # 热更新包输出目录
└── README.md                     # 详细文档
```

## 🚀 开始构建

### 方式一：使用 Android Studio（推荐）

1. **下载并安装 Android Studio**
   - 官网：https://developer.android.com/studio

2. **打开项目**
   ```
   File → Open → 选择 StudyX-Android 文件夹
   ```

3. **等待 Gradle 同步**
   - 首次打开会自动下载依赖，请耐心等待

4. **构建 APK**
   ```
   Build → Build Bundle(s) / APK(s) → Build APK(s)
   ```

5. **找到 APK 文件**
   ```
   app/build/outputs/apk/debug/app-debug.apk
   ```

### 方式二：命令行构建（需要配置环境）

```bash
# 进入项目目录
cd StudyX-Android

# Windows 运行构建脚本
build_apk.bat

# 或者使用 Gradle
./gradlew assembleDebug      # 调试版
./gradlew assembleRelease    # 发布版
```

## 📱 安装到手机

### 方法 1：USB 调试

1. 手机开启开发者模式（设置 → 关于手机 → 连续点击版本号）
2. 开启 USB 调试（设置 → 开发者选项 → USB 调试）
3. 连接电脑，允许调试
4. 使用 ADB 安装：
   ```bash
   adb install -r app/build/outputs/apk/debug/app-debug.apk
   ```

### 方法 2：直接传输

1. 将 APK 文件发送到手机（微信、QQ、数据线等）
2. 在手机上点击安装
3. 如果提示未知来源，请到设置中允许

## 🔄 配置热更新（可选）

### 1. 准备服务器

你需要一个可以托管文件的服务器，支持 HTTP/HTTPS 访问。

### 2. 修改更新地址

编辑 `app/src/main/java/com/studyx/app/MainActivity.java`：

```java
// 第 37 行左右
private static final String UPDATE_URL = "https://你的服务器地址/studyx/update.json";
```

### 3. 制作更新包

运行 `make_update.bat`，按提示输入版本信息。

### 4. 上传文件

将生成的两个文件上传到服务器：
- `updates/studyx_v1.x.x.zip` - 更新包
- `updates/update.json` - 版本信息

### 5. 测试更新

在 App 中触发检查更新，或重新启动 App。

## 🎨 自定义

### 修改应用名称

编辑 `app/src/main/res/values/strings.xml`：
```xml
<string name="app_name">你的应用名称</string>
```

### 修改应用图标

替换以下目录中的图标文件（PNG 格式）：
- `mipmap-xxxhdpi/` (192x192)
- `mipmap-xxhdpi/` (144x144)
- `mipmap-xhdpi/` (96x96)
- `mipmap-hdpi/` (72x72)
- `mipmap-mdpi/` (48x48)

### 修改版本号

编辑 `app/build.gradle`：
```gradle
android {
    defaultConfig {
        versionCode 1           // 数字版本号，递增
        versionName "1.0.0"     // 显示版本名
    }
}
```

## 🔧 常见问题

### Q: Gradle 同步失败？
**A:** 
1. 检查网络连接
2. 检查 Android SDK 路径设置（File → Settings → Appearance & Behavior → System Settings → Android SDK）
3. 尝试 File → Invalidate Caches → Invalidate and Restart

### Q: 构建失败，提示找不到符号？
**A:** 
1. 确保 JDK 11 或更高版本
2. 尝试 Clean Project：Build → Clean Project
3. 重新同步 Gradle：File → Sync Project with Gradle Files

### Q: App 安装后白屏？
**A:** 
1. 检查 `assets/www/index.html` 是否存在
2. 查看 Logcat 日志（Android Studio 底部工具栏）
3. 确保 WebView 已启用 JavaScript

### Q: 热更新不生效？
**A:** 
1. 检查 UPDATE_URL 是否正确
2. 确保服务器可访问
3. 检查版本号是否已递增
4. 查看 Logcat 中的 "UpdateManager" 日志

### Q: 如何禁用热更新？
**A:** 
将 MainActivity.java 中的 `checkForUpdate()` 调用注释掉：
```java
// checkForUpdate();  // 注释掉这一行
```

## 📚 进阶功能

### JavaScript 调用原生功能

```javascript
// 在网页 JavaScript 中
if (typeof Android !== 'undefined') {
    Android.showToast("来自网页的消息");
    Android.vibrate(100);
}
```

### 添加新功能

1. 在 `WebAppInterface.java` 中添加新方法
2. 添加 `@JavascriptInterface` 注解
3. 在网页中通过 `Android.方法名()` 调用

## 📞 技术支持

如有问题，请查看：
- 详细文档：README.md
- Android 官方文档：https://developer.android.com/docs

---

**祝你构建顺利！** 🎉
